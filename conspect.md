# Conspect

## Выделение текста Mark Down

**Полужирный**  две звездочки по обе стороны текста это полужирный

*Курсив*  одна звездочка по обе стороны текста это курсив

* Элемент 1 звездочка и пробел перед текстом, это элемент 1

Решетка и пробел перед текстом это заголовок

Две решетки и пробел перед текстом это под заголовок

как показано выше в первых двух строках.

Зачеркнутый текст тильда перед и после текста ( Вопрос )

- Показать уровень заголовка это тире пробел текст

    Таб это отступ вложенные списки

## Comands

- git init позволяет из ппапки сделать репозитарий

- git add позволяет добавить файл к отслеживанию git

- git commit сохряняет текущее состояние

- git diff сравнение

- git log журнал всех изменений коммитов

- git checkout и первые четыре и более строки коммита позволяет вернуться к состоянию коммита

- git checkout master позволяет вернуться в текущее состояние, к ветке мамастер
